<% if current_user.id == 1 %>

<h1>Novo Produto</h1>

<%= render 'form', product: @product %>

<% else %>

<p>Apenas o administrador pode criar um produto</p>
<%= link_to "Sair", destroy_user_session_path, method: :delete, :class => 'btn btn-danger'  %>

<% end %>

<br>
<%= link_to 'Voltar', products_path %>
